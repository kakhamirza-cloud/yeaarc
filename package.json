{
  "name": "arc-mfers-mint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:art": "node scripts/sync-art.mjs",
    "predev": "node scripts/sync-art.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-art.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=arcmfers",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami"
  },
  "dependencies": {
    "ethers": "^6.14.3"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.24.0"
  }
}
