{
  "name": "arc-mfers-mint",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:art": "node scripts/sync-art.mjs",
    "predev": "node scripts/sync-art.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-art.mjs",
    "build": "vite build",
    "build:cf": "node scripts/build-cf.mjs",
    "preview": "vite preview",
    "deploy": "npm run build:cf && wrangler deploy",
    "deploy:pages": "npm run build:cf && wrangler pages deploy dist --project-name=yeaarc",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami"
  },
  "dependencies": {
    "ethers": "^6.14.3"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.24.0"
  }
}
