# ARC mfers — Cloudflare static deploy
name = "arcmfers"
compatibility_date = "2026-07-18"

# Required by current wrangler deploy (Workers static assets)
[assets]
directory = "./dist"
not_found_handling = "404-page"
